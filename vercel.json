{
  "buildCommand": "npm run build -w @finmark/shared && npm run build -w @finmark/web",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://finmark-api.up.railway.app/api/:path*"
    }
  ]
}
