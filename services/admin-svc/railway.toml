[build]
builder = "dockerfile"
dockerfilePath = "services/admin-svc/Dockerfile"

[deploy]
startCommand = "node services/admin-svc/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
numReplicas = 1
