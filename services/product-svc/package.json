{
  "name": "@finmark/product-svc",
  "version": "1.0.0",
  "description": "Product service — catalog management and SQS order event consumer",
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon --exec \"ts-node --transpile-only --project tsconfig.json\" src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.1063.0",
    "@finmark/aws": "*",
    "@finmark/db": "*",
    "@finmark/shared": "*",
    "express": "^4.21.2",
    "zod": "^3.25.64"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
