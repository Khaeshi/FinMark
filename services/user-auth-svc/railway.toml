[build]
builder = "dockerfile"
dockerfilePath = "services/user-auth-svc/Dockerfile"

[deploy]
startCommand = "node services/user-auth-svc/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
numReplicas = 1
