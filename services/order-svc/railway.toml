[build]
builder = "dockerfile"
dockerfilePath = "services/order-svc/Dockerfile"

[deploy]
startCommand = "node services/order-svc/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
numReplicas = 1
